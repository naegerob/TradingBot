ktor:
    deployment:
        port: 8080
        sslPort: 8080
        ssl:
            keyStore: "${KEYSTORE}/tls.p12"
            keyAlias: "myCert"
            keyStorePassword: "changeit"
            privateKeyPassword: "changeit"

    application:
        modules:
            - com.example.ApplicationKt.module
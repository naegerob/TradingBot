ktor:
    deployment:
        sslPort: 8081
    security:
        ssl:
            keyStore: security/keystore.jks
            keyAlias: myAlias
            keyStorePassword: password
            privateKeyPassword: password

    application:
        modules:
            - com.example.ApplicationKt.module